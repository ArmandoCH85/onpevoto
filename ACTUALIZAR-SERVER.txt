# Actualizar ONPE Voto en el servidor

cd /opt/onpevoto
sudo git pull
sudo npm install
sudo systemctl restart onpevoto

# Verificar
sudo systemctl status onpevoto
curl http://localhost:3000

# Si hay que descargar la DB GeoIP (primera vez):
cd /opt/onpevoto/geoip
# Ver paso 4b del archivo INSTALACION-UBUNTU-20.04.txt
sudo chown -R www-data:www-data /opt/onpevoto
